# Базовый URL бэкенда. В dev-режиме не нужен — запросы идут через прокси
# Vite (см. vite.config.ts) на http://localhost:8080.
# Задайте для production-сборки, если фронт и бэк на разных доменах:
# VITE_API_BASE_URL=https://api.example.com
